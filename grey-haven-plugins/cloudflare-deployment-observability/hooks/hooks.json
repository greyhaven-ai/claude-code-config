{
  "description": "Cloudflare deployment monitoring and CI/CD pipeline observability. Stop hooks moved to agent-scoped (v2.1.0). Updated for Claude Code v2.0.74+.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-cloudflare-status.sh",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
